%h1{align: "center"}= @wiki.markdown_title

%br/
.row
  .col-md-8.col-md-offset-2

    .well
      = @wiki.markdown_body

- if policy(@wiki).edit?
  %br/
  .row{align: "center"}
    = link_to(edit_wiki_path(@wiki), class: 'btn btn-primary') do
      %i.glyphicon.glyphicon-pencil.glyphicon-white
      edit
